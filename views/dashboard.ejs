<!-- views/dashboard.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <!-- ... (head content) -->
  <title>User Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body class="bg-light">

  <div class="container mt-5">
    <h1 class="text-info mb-4">User Dashboard</h1>

    <% if (authenticated) { %>
      <p class="lead">Welcome, <%= user.username %>!</p>
    <% } %>

    <!-- Navigation links -->
    <div class="mb-3">
      <a href="/api/profile" class="btn btn-primary me-2">View Profile</a>
      <a href="/api/logout" class="btn btn-danger me-2">Logout</a>

      <!-- Conditional link to home page -->
      <% if (authenticated) { %>
        <a href="/api" class="btn btn-secondary">Go to Home</a>
      <% } %>
    </div>
  </div>

</body>
</html>
